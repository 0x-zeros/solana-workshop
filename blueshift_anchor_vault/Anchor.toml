[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.localnet]
blueshift_anchor_vault = "FPgL6qqt53QjRovkxAGeX8j8jh31dNvXABwt6CsCeRA3"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

# When using surfpool (or existing validator): start surfpool first, then run:
#   yarn test:surfpool   (or: anchor test --skip-local-validator)
[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 \"tests/**/*.ts\""
